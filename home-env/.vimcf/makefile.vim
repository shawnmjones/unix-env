set filetype=make
"cal SetSyn("make") " so syntax highlighting displays properly
set tabstop=8 " so I can tell the tabs from the four spaces elsewhere
set noexpandtab " tabs are actually required by Makefiles
set syntax=make
